import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
      domains: ['i.postimg.cc','cdn.sanity.io','images.unsplash.com','picsum.photos'],
  }
};

export default nextConfig;
